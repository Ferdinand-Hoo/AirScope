# Support presets and expert security settings

The Management Interface will offer named Security Profiles for common valid
combinations and an expert mode for selecting authentication, pairwise cipher,
PMF, and SAE-PWE behavior independently. Both paths produce the same validated
configuration model, and combinations rejected by the WiFi platform must be
blocked before they can become the Persistent Configuration.
