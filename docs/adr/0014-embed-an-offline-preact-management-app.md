# Embed an offline Preact management app

The Management Interface will be a Preact and TypeScript single-page
application built with Vite, compressed and embedded in the firmware with all
dependencies pinned and no CDN or Internet runtime dependency. It will consume
only the versioned Management API and provide responsive, client-side feedback
for configuration constraints while the firmware remains the authoritative
validator.
