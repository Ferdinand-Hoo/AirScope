# Operate as a standalone access point

The platform will run only as a standalone WiFi access point and will not join
an upstream WiFi network, provide NAT, or route Internet traffic. Management is
available only from the local experimental network, while USB is reserved for
power, firmware installation, and diagnostics; this prevents an upstream
station connection from taking control of the SoftAP channel.
